{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Autonomous Mobile Robots Bootcamp","text":"<p>By Pranav A. Bhounsule \u00b7 pranav@uic.edu</p> <p>An example of a sample outcome of the bootcamp.</p>"},{"location":"#purpose","title":"Purpose","text":"<p>The purpose of this module is two-fold: first, to learn about autonomous mobile robot navigation, and second, to practice the workflow of taking an idea from concept, to simulation, to hardware.</p> <p>The module has two stages:</p> <ul> <li>Stage 1 \u2014 you learn autonomous mobile robot navigation concepts and implement   control in simulation. Simulation lets you try many ideas quickly and cheaply, and   stress-test an idea under a variety of conditions before committing to it.</li> <li>Stage 2 \u2014 you take an idea that worked in simulation to hardware, where the   testing you already did in simulation greatly increases the chance of success.</li> </ul> <p>Stage 1 is self-paced and done at home. Stage 2 takes place at the University \u2014 typically over a weekend \u2014 where you implement your controller on real hardware.</p>"},{"location":"#outcomes","title":"Outcomes","text":"<p>At the end of these exercises, students will be able to:</p> <ol> <li>(Understand) Explain how common autonomous-navigation frameworks \u2014    heuristic controllers, artificial potential fields, timed elastic bands, the    dynamic window approach, and model predictive control \u2014 decide how a robot    should move to reach a goal while avoiding obstacles.</li> <li>(Understand) Describe how lidar and RGB-D cameras sense a robot's    surroundings, and what data each sensor returns.</li> <li>(Understand) Explain how mecanum wheels combine four independently driven    wheels to produce omni-directional motion.</li> <li>(Apply) Implement a go-to-goal controller with obstacle avoidance in    simulation, using lidar and/or RGB-D camera data.</li> <li>(Analyze) Compare the performance of a heuristic controller against a    principled controller (DWA, artificial potential field, timed elastic band, or    MPC) across simple and BARN benchmark environments.</li> <li>(Create) Design, build, and deploy a working autonomous-navigation    controller \u2014 from concept, through simulation, to real hardware.</li> </ol>"},{"location":"#program-at-a-glance","title":"Program at a Glance","text":"Stage Where Pace Page Part 1 \u2014 Motion Planning in Simulation At home Self-paced Part 1 Part 2 \u2014 Motion Planning on Hardware At the University (typically a weekend) In person Part 2"},{"location":"#help-errata","title":"Help / Errata","text":"<p>If you have any questions, hit road blocks, or run into any issues with these instructions \u2014 including dead links \u2014 feel free to email pranav@uic.edu.</p>"},{"location":"#disclaimer","title":"Disclaimer","text":"<p>We don't guarantee every student will finish every stage. But even completing parts of the bootcamp is enough to walk away with new skills.</p>"},{"location":"part1-simulation/","title":"Part 1: Motion Planning in Simulation","text":""},{"location":"part1-simulation/#equipment-list-for-software","title":"Equipment List for Software","text":"<ol> <li>Microsoft Visual Studio Code for Python. Download and install from    Microsoft Visual Studio Code. Set up    Python using this video and test a simple script the same way it's shown there:    youtu.be/9o4gDQvVkLU.</li> <li>Install additional Python packages. Packages can be installed by typing the    following in the \"terminal\" in Visual Studio Code:</li> </ol> <pre><code>python3 -m pip install {package_name}\n</code></pre> <p>Run this once for each package. For <code>{package_name}</code>, install <code>numpy</code> and    <code>matplotlib</code>. This minute-long video shows how to install a package:    youtu.be/sQPgR1Fz67c.   3. Multi-Joint dynamics with Contact (MuJoCo). Install the MuJoCo Python package    by typing the following in the terminal inside Visual Studio Code:</p> <p><pre><code>python3 -m pip install mujoco\n</code></pre> 4. Vibe coding with LLMs. It's highly recommended that you use LLMs (ChatGPT,    Gemini, Claude, etc.) to vibe code. Vibe coding is a style of software creation    where you use natural-language prompts to direct an LLM to write, fix, and build    code, rather than typing it yourself line by line. You don't need a paid version    of any LLM \u2014 the free tier is enough.</p> <p>General advice: break the problem down into simple tasks, then assign the LLM to    program each task one at a time. Run multiple tests after each task to make sure    the output does what you expect, and save your working code before you ask the    LLM to add more features.</p>"},{"location":"part1-simulation/#understanding-the-files-given-to-you","title":"Understanding the Files Given to You","text":""},{"location":"part1-simulation/#overview","title":"Overview","text":"<p>Download the folder <code>autonav.zip</code> and unzip it. You'll see the Python file <code>template_mecanum.py</code>. It calls into a set of routines in <code>MentorPi_hw</code> (for hardware, covered in Part 2) and <code>MentorPi_sw</code> (for the simulator). In this section we work only with simulation:</p> <pre><code>PLATFORM = \"sw\"\n</code></pre>"},{"location":"part1-simulation/#motion","title":"Motion","text":"<p>Mecanum wheels let a robot drive forward/backward, sideways, and turn \u2014 all independently \u2014 by spinning four rollered wheels at different speeds. Read about how they work here: Moving with Mecanum Wheels (SunFounder).</p> <p>You command the robot's motion with three numbers: forward/backward speed <code>vx</code>, sideways speed <code>vy</code>, and turning rate <code>wz</code>. For example:</p> <pre><code>vx = 1\nvy = 0.5\nwz = 1.0\nrobot.set_velocity(vx=vx, wz=wz, vy=vy)\n</code></pre> <p>Each run also defines a goal position in its scene XML file, which you can read with:</p> <pre><code>goal_x, goal_y = robot.get_goal()\n</code></pre> <p>and the robot's own position and heading at any instant come from:</p> <pre><code>x, y, theta = robot.get_pose()\n</code></pre>"},{"location":"part1-simulation/#lidar-sensor","title":"Lidar Sensor","text":"<p>A 2D lidar spins a laser beam around the robot and measures the distance to whatever it hits at each angle, building a ring of range readings around the robot. Read more here: LiDAR Processing Overview (MathWorks).</p> <p>Turn the lidar on with:</p> <pre><code>flag_lidar = True\n</code></pre> <p>and read/visualize it with:</p> <pre><code>points = lidar.get_data()\nlidar.show()\n</code></pre> <p><code>points</code> holds every point the lidar detected on that sweep, given in the robot's own frame (not the world frame): <code>points.x_data</code> is meters ahead of the robot, <code>points.y_data</code> is meters to the left (positive) or right (negative), and <code>points.angle_data</code> is the angle to that point in degrees, where 0\u00b0 is straight ahead and positive angles are counterclockwise (to the left). All three are lists of the same length \u2014 one entry per detected point \u2014 so <code>points.x_data[i]</code>, <code>points.y_data[i]</code>, <code>points.angle_data[i]</code> together describe the <code>i</code>-th detected point, and <code>len(points.x_data)</code> tells you how many points were detected. Beams that hit nothing within <code>lidar_range_to_see_in_meters</code> are left out, so you won't get a fixed number of points every call \u2014 check for an empty result before indexing into it.</p> <p>Run the simulation and you'll see the lidar view appear. You can tune these parameters in the code:</p> <pre><code>lidar_field_of_view_degrees = 90.0   # angular span the lidar scans, centered on the robot's forward direction\nlidar_range_to_see_in_meters = 3.0   # maximum distance, in meters, the lidar can detect an obstacle\nlidar_num_beams = 180                # number of individual beams spread across the field of view (angular resolution)\n</code></pre>"},{"location":"part1-simulation/#camera-sensor","title":"Camera Sensor","text":"<p>An RGB-D camera captures a normal color (RGB) image plus a depth image, where each pixel's value is its distance from the camera instead of its color. Read more here: RGB-D Images: A Comprehensive Overview (GeeksforGeeks).</p> <p>Turn the camera on with:</p> <pre><code>flag_camera = True\n</code></pre> <p>and read/visualize it with:</p> <pre><code>frame = camera.get_data()\ncamera.show()\n</code></pre> <p><code>frame</code> holds the RGB and depth images captured together, plus a convenience reading:</p> <ul> <li><code>frame.rgb</code> \u2014 a <code>(camera_height, camera_width, 3)</code> array of <code>0-255</code> color   values (a normal color image).</li> <li><code>frame.depth</code> \u2014 a <code>(camera_height, camera_width)</code> array where each pixel is   that point's distance from the camera plane, in meters (not a 0-255 value).   This is depth, not straight-line range from the camera \u2014 the two only agree   exactly at the center pixel.</li> <li><code>frame.center_depth</code> \u2014 a single number: the depth, in meters, at the pixel   right in the center of the frame. Handy when you just want \"how far away is   whatever the robot is looking straight at\" without indexing into <code>frame.depth</code>   yourself.</li> </ul> <p>Run the simulation and you'll see the camera view \u2014 both RGB and depth. You can tune these parameters in the code:</p> <pre><code>camera_width = 320    # captured frame width, in pixels\ncamera_height = 240   # captured frame height, in pixels\n</code></pre>"},{"location":"part1-simulation/#exercises-in-simulation","title":"Exercises in Simulation","text":"<p>The objective is to avoid obstacles while reaching a goal. Obstacle positions come from the lidar and camera as distances from the robot in the local (robot) frame \u2014 that is, measured relative to the robot. The goal position is given in the world frame \u2014 that is, measured relative to the world.</p>"},{"location":"part1-simulation/#a-heuristic-go-to-goal-controller","title":"A heuristic go-to-goal controller","text":"<p>\"Heuristic\" means a controller built from practical experience or intuition rather than a formal optimization. One control idea is given below \u2014 feel free to try your own instead.</p> <p>You have two natural choices of control inputs: <code>vx, vy</code> (straight + sideways motion) or <code>vx, wz</code> (straight motion + turning). Start with <code>vx, vy</code> \u2014 it's easier to reason about \u2014 and extend to <code>vx, wz</code> afterward if you'd like.</p> <p>A simple way to steer toward the goal is to compute the direction from the robot's current position \\((x, y)\\) to the goal \\((x_g, y_g)\\):</p> \\[ \\phi_g = \\operatorname{atan2}(y_g - y,\\; x_g - x) \\] <p>(subtract the robot's heading \\(\\theta\\) if you want this angle expressed in the robot's own body frame instead of the world frame). Then set the ratio of sideways to forward speed from that angle:</p> \\[ \\frac{v_y}{v_x} = \\tan(\\phi_g) \\] <p>Set <code>vx</code> to a small value, e.g. <code>vx = 0.1</code>, and compute <code>vy = vx * tan(phi_g)</code>. Once this works, ramp up <code>vx</code> to get the robot moving faster.</p>"},{"location":"part1-simulation/#adding-obstacle-avoidance","title":"Adding obstacle avoidance","text":"<p>Extend the same idea to avoid obstacles. Compute the direction from the robot's current position to the nearest detected obstacle, \\(\\phi_d\\), and steer using:</p> \\[ \\frac{v_y}{v_x} = \\tan(\\phi_g + \\delta) \\] <p>where \\(\\delta\\) is an offset angle you choose from roughly \\(-90\u00b0\\) to \\(90\u00b0\\) (but not \\(0\u00b0\\)), with its sign chosen based on which side the obstacle is on. This biases the robot's heading away from the obstacle while it continues generally toward the goal.</p> <p>Suggested order of attack:</p> <ol> <li>Start with the lidar sensor and command <code>vx, vy</code> as shown above.</li> <li>Once that works, swap the lidar sensor for the camera.</li> <li>Try the <code>vx, wz</code> control combination as well.</li> </ol>"},{"location":"part1-simulation/#adding-more-obstacles","title":"Adding more obstacles","text":"<p>Once your controller works, add more obstacles by modifying <code>env_example.xml</code>.</p>"},{"location":"part1-simulation/#submit-your-work","title":"Submit your work","text":"<p>At this point, zip your files and email them to pranav@uic.edu. We'll meet next to talk about standardized testing, described below.</p>"},{"location":"part1-simulation/#standardized-testing-with-barn","title":"Standardized testing with BARN","text":"<p>Once you're ready, test your controller on standardized environments from BARN \u2014 Benchmark Autonomous Robot Navigation. BARN is a public benchmark of 300 procedurally generated, simulated obstacle courses spanning a range of difficulty (from sparse obstacles to tightly packed, maze-like clutter), used to compare ground-robot navigation approaches on a common, standardized set of environments.</p> <p>To generate an environment, set the <code>BARN_num</code> variable in <code>barn_to_mujoco.py</code> and run the script. This generates the environment inside the <code>BARN</code> folder. Copy the path to the resulting <code>scene_&lt;no&gt;.xml</code> into your Python script and run it, where <code>&lt;no&gt;</code> is the BARN environment number.</p>"},{"location":"part1-simulation/#try-a-principled-controller","title":"Try a principled controller","text":"<p>After you're comfortable with the heuristic controller on simple environments and on BARN, pick one of these more principled controllers to implement:</p> <ul> <li>Dynamic Window Approach (DWA)</li> <li>Artificial Potential Field</li> <li>Timed Elastic Band</li> <li>Model Predictive Control</li> </ul> <p>Program it against the simple environment first, then against BARN, and compare its performance to your heuristic controller.</p>"},{"location":"part2-hardware/","title":"Part 2: Motion Planning on Hardware","text":""},{"location":"part2-hardware/#equipment-list-for-hardware","title":"Equipment List for Hardware","text":"<p>Not all of these are free.</p> <ol> <li>Microsoft Visual Studio Code for Python. Download and install from    Microsoft Visual Studio Code. Set up    Python using this video and test a simple script the same way it's shown there:    youtu.be/9o4gDQvVkLU.</li> <li>Mobile robot with mecanum wheels. A MentorPi M1 mobile robot with a    Raspberry Pi (at least 4 GB RAM), an RGB-D camera, and a lidar.</li> <li>Connecting to and running the Raspberry Pi. A VNC viewer (free for 30 days)    if you want to access the robot from your own computer. If you'd rather not use    a VNC viewer, you'll need a monitor, keyboard, an HDMI-to-mini-USB cable, and a    mouse to access the Raspberry Pi directly.</li> <li>Vibe coding with LLMs. It's highly recommended that you use LLMs (ChatGPT,    Gemini, Claude, etc.) to vibe code. Vibe coding is a style of software creation    where you use natural-language prompts to direct an LLM to write, fix, and build    code, rather than typing it yourself line by line. You don't need a paid version    of any LLM \u2014 the free tier is enough.</li> </ol> <p>General advice: break the problem down into simple tasks, then assign the LLM to    program each task one at a time. Run multiple tests after each task to make sure    the output does what you expect, and save your working code before you ask the    LLM to add more features.</p>"},{"location":"part2-hardware/#accessing-the-raspberry-pi","title":"Accessing the Raspberry Pi","text":"<p>You can access the Raspberry Pi in two ways:</p> <ol> <li>From your own computer. Download and install a VNC viewer, then get the IP    address, username, and password of the Raspberry Pi from us.</li> <li>Directly on the Pi. Connect the Pi to a monitor with an HDMI-to-mini-USB    cable, and use a wireless keyboard/mouse combo.</li> </ol>"},{"location":"part2-hardware/#running-the-robot","title":"Running the Robot","text":"<p>Power the robot using the switch on top. On the Raspberry Pi, open a terminal and run:</p> <pre><code>cd Desktop\n./run.sh\n</code></pre> <p>This takes you to the folder containing the files you'll modify. To run the test file:</p> <pre><code>python3 template_mecanum.py\n</code></pre> <p>This displays the lidar and camera views on screen, prints the robot's <code>x, y, theta</code> position, and moves the robot. You'll modify this file to implement autonomous navigation.</p>"},{"location":"part2-hardware/#writing-and-transferring-code","title":"Writing and Transferring Code","text":"<p>You can modify <code>template_mecanum.py</code> directly on the Raspberry Pi, building on the work you already did there. Alternately, you can edit the file on your own computer and transfer it to the Pi using FileZilla.</p> <p>FileZilla needs the Pi's IP address, username, and password, and connects on port 22. On the left side of the FileZilla window are your computer's files; on the right are the Pi's files. Navigate to:</p> <pre><code>/home/pi/docker/tmp/template_mentorpi\n</code></pre> <p>and drag and drop files from your computer to the Pi.</p>"},{"location":"part2-hardware/#exercises-on-hardware","title":"Exercises on Hardware","text":"<p>Follow the same progression as Part 1 to develop and deploy code on hardware:</p> <ol> <li>Start simple: use only <code>vx, vy</code> control with the lidar for obstacle    avoidance.</li> <li>Once that works, change the drive mode (e.g. add <code>wz</code>) and/or switch to the    camera.</li> <li>Build up more complex behavior strategically, saving old working code as you go    before adding new features.</li> <li>Take plenty of photos and videos of your robot in action \u2014 you'll want them    later (including for the optional write-up).</li> </ol>"}]}